<h1><%=@category.name%></h1>

<% @category.contractors.each do |contractor|  %>
  <p><%= contractor.name %></p>
  <% if user_signed_in? %>
    <p><%= link_to "Solicitar orçamento", new_contractor_estimate_path(contractor)%>
  <% end %>
<%end%>

<p><%= link_to 'Editar', edit_category_path %></p>
<p><%= link_to 'Voltar', root_path %>
